# Настройка Supabase для Beauty CRM

Пошаговая инструкция по настройке базы данных и аутентификации.

## 1. Создание проекта Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Зарегистрируйтесь или войдите в аккаунт
3. Нажмите "New project"
4. Заполните форму:
   - **Name**: beauty-crm (или любое другое имя)
   - **Database Password**: придумайте надежный пароль и сохраните его
   - **Region**: выберите ближайший к вам регион
   - **Pricing Plan**: Free (для начала)
5. Нажмите "Create new project"
6. Дождитесь создания проекта (2-3 минуты)

## 2. Получение ключей API

1. После создания проекта перейдите в раздел **Settings** (⚙️) → **API**
2. Найдите и скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (очень длинная строка)

3. Обновите файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_ключ_здесь
```

## 3. Создание схемы базы данных

### Способ 1: Через SQL Editor (Рекомендуется)

1. В панели Supabase перейдите в **SQL Editor** (📝)
2. Нажмите "New query"
3. Откройте файл `supabase/migrations/001_initial_schema.sql` в вашем редакторе
4. Скопируйте **весь код** из этого файла
5. Вставьте код в SQL Editor в Supabase
6. Нажмите "Run" или нажмите `Ctrl+Enter` (⌘+Enter на Mac)
7. Дождитесь завершения выполнения (может занять 10-20 секунд)

### Способ 2: Через Supabase CLI (Для продвинутых)

```bash
# Установите Supabase CLI
npm install -g supabase

# Войдите в аккаунт
supabase login

# Свяжите проект
supabase link --project-ref your_project_ref

# Примените миграции
supabase db push
```

## 4. Проверка создания таблиц

1. Перейдите в **Table Editor** (📊)
2. Вы должны увидеть следующие таблицы:
   - ✅ salons
   - ✅ salon_users
   - ✅ clients
   - ✅ service_categories
   - ✅ services
   - ✅ staff_members
   - ✅ staff_services
   - ✅ appointments
   - ✅ product_categories
   - ✅ products
   - ✅ product_usage
   - ✅ transactions
   - ✅ visit_history

3. Проверьте, что Row Level Security (RLS) включен:
   - Откройте любую таблицу
   - Справа вверху должен быть включен переключатель "RLS enabled"

## 5. Настройка аутентификации

### Включение Email/Password аутентификации:

1. Перейдите в **Authentication** → **Providers**
2. Найдите "Email" в списке провайдеров
3. Убедитесь, что переключатель включен
4. Настройки по умолчанию подходят для разработки

### Создание тестового пользователя:

1. Перейдите в **Authentication** → **Users**
2. Нажмите "Add user" → "Create new user"
3. Заполните:
   - **Email**: test@beauty-crm.com
   - **Password**: Test123456!
   - **Auto Confirm User**: ✅ (включите для тестирования)
4. Нажмите "Create user"

## 6. Создание тестовых данных салона

После создания пользователя, нужно создать запись в таблице `salons` и `salon_users`:

1. Перейдите в **SQL Editor**
2. Создайте новый запрос и выполните:

```sql
-- Создаем салон
INSERT INTO salons (name, address, phone, email)
VALUES (
  'Салон "Красота"',
  'г. Москва, ул. Примерная, д. 1',
  '+7 (999) 123-45-67',
  'info@beauty-salon.com'
);

-- Получаем ID созданного салона
SELECT id FROM salons ORDER BY created_at DESC LIMIT 1;

-- Привязываем пользователя к салону
-- ЗАМЕНИТЕ 'user_id_здесь' на ID пользователя из auth.users
-- ЗАМЕНИТЕ 'salon_id_здесь' на ID салона из предыдущего запроса
INSERT INTO salon_users (salon_id, auth_user_id, email, full_name, role)
VALUES (
  'salon_id_здесь',
  'user_id_здесь',
  'test@beauty-crm.com',
  'Тестовый Пользователь',
  'owner'
);
```

### Где найти user_id:

1. **Authentication** → **Users**
2. Кликните на пользователя
3. Скопируйте значение поля "UID"

## 7. Заполнение демо-данными (Опционально)

Для быстрого старта можно добавить демо-данные:

```sql
-- Предположим salon_id = 'your-salon-id'

-- Добавляем категории услуг
INSERT INTO service_categories (salon_id, name) VALUES
('your-salon-id', 'Маникюр и педикюр'),
('your-salon-id', 'Парикмахерские услуги'),
('your-salon-id', 'Косметология');

-- Добавляем услуги
INSERT INTO services (salon_id, name, description, duration, price, cost) VALUES
('your-salon-id', 'Маникюр классический', 'Классический маникюр с покрытием', 60, 1500, 300),
('your-salon-id', 'Стрижка женская', 'Женская стрижка любой сложности', 90, 2500, 200),
('your-salon-id', 'Чистка лица', 'Комплексная чистка лица', 120, 3500, 500);

-- Добавляем мастеров
INSERT INTO staff_members (salon_id, first_name, last_name, phone, email, specialization, color) VALUES
('your-salon-id', 'Анна', 'Иванова', '+7 (999) 123-45-67', 'anna@example.com', 'Мастер по маникюру', '#3b82f6'),
('your-salon-id', 'Мария', 'Петрова', '+7 (999) 234-56-78', 'maria@example.com', 'Парикмахер', '#ec4899'),
('your-salon-id', 'Елена', 'Сидорова', '+7 (999) 345-67-89', 'elena@example.com', 'Косметолог', '#8b5cf6');

-- Добавляем клиентов
INSERT INTO clients (salon_id, first_name, last_name, phone, email, loyalty_tier) VALUES
('your-salon-id', 'Ольга', 'Смирнова', '+7 (999) 111-11-11', 'olga@example.com', 'gold'),
('your-salon-id', 'Наталья', 'Козлова', '+7 (999) 222-22-22', 'natalia@example.com', 'silver');
```

## 8. Проверка работы

1. Запустите приложение:
   ```bash
   npm run dev
   ```

2. Откройте [http://localhost:3000](http://localhost:3000)

3. Войдите с тестовыми данными:
   - Email: test@beauty-crm.com
   - Password: Test123456!

## 9. Настройка безопасности для production

Перед запуском в production:

### 1. Настройте Email подтверждение:
- **Authentication** → **Email Templates**
- Настройте шаблоны писем

### 2. Настройте домены:
- **Authentication** → **URL Configuration**
- Добавьте ваши production домены в "Site URL" и "Redirect URLs"

### 3. Настройте Rate Limiting:
- **Authentication** → **Rate Limits**
- Установите лимиты на попытки входа

### 4. Проверьте RLS политики:
- Убедитесь, что все таблицы защищены RLS
- Проверьте, что пользователи видят только свои данные

## 10. Мониторинг и резервное копирование

### Database Backups:
- **Database** → **Backups**
- В бесплатном плане автоматические бэкапы не включены
- Рекомендуется настроить ручное резервное копирование

### Monitoring:
- **Database** → **Logs**
- Просмотр логов запросов и ошибок

## Troubleshooting

### Проблема: "No rows returned" при входе
**Решение**: Убедитесь, что:
1. Пользователь создан в auth.users
2. Есть соответствующая запись в salon_users
3. salon_users.auth_user_id совпадает с UID пользователя

### Проблема: "Row Level Security policy violation"
**Решение**: 
1. Проверьте, что функция `get_user_salon_id()` создана
2. Убедитесь, что у пользователя есть запись в salon_users
3. Проверьте RLS политики в Table Editor

### Проблема: Таблицы не создаются
**Решение**:
1. Проверьте синтаксис SQL в SQL Editor
2. Выполняйте миграцию по частям, если возникают ошибки
3. Проверьте логи в разделе Logs

## Полезные ссылки

- [Документация Supabase](https://supabase.com/docs)
- [Row Level Security Guide](https://supabase.com/docs/guides/auth/row-level-security)
- [Supabase CLI](https://supabase.com/docs/guides/cli)
- [Next.js + Supabase Guide](https://supabase.com/docs/guides/getting-started/quickstarts/nextjs)

---

**Готово!** 🎉 Теперь ваша CRM система подключена к базе данных Supabase!
